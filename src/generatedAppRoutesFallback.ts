import type { ReactNode } from '@lynx-js/react'

export function TamerGeneratedAppRoutes(): ReactNode {
  throw new Error(
    'Enable `tamerRouterPlugin({ root: "src/pages" })` so `@tamer4lynx/tamer-router/generated-app-routes` resolves to codegen output.',
  )
}

export const TAMER_KNOWN_PATHS_FULL: string[] = []
export const TAMER_KNOWN_PATHS_COORDINATOR: string[] = []
export const TAMER_KNOWN_PATHS: string[] = []
